<div class="btn-group">
  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Insurer
  </button>
  <div class="dropdown-menu">
    <% @insurers.each do |insurer| %>
      <a class="dropdown-item" href="<%= hospitals_path(@hospital, tag: insurer)%>"><%= insurer%></a>
    <% end %>
  </div>
</div>
<div class="btn-group">
  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Provider Type
  </button>
  <div class="dropdown-menu">
    <% @provider_types.each do |type| %>
      <a class="dropdown-item" href="<%= hospitals_path(@hospital, tag: type)%>"><%=  type%></a>
    <% end %>
  </div>
</div>
<div class="btn-group">
  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Language
  </button>
  <div class="dropdown-menu">
    <% @languages.each do |language| %>
      <a class="dropdown-item" href="<%= hospitals_path(@hospital, tag: language)%>"><%= language %></a>
    <% end %>
  </div>
</div>
<div class="btn-group">
  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Specialty
  </button>
  <div class="dropdown-menu">
    <% @specialties.each do |specialty| %>
      <a class="dropdown-item" href="<%= hospitals_path(@hospital, tag: specialty)%>"><%= specialty %></a>
    <% end %>
  </div>
</div>