<!-- comments/show.html.erb -->
<br>
<%= simple_form_for [@hospital, @comment] do |f| %>
  <%= f.input :title %>
  <%= f.input :comment %>
  <%= f.button :submit, class: "btn btn-primary review-button" %>
<% end %>